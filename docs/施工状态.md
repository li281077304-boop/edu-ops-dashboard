# 施工状态

| 阶段 | 状态 | 说明 |
|---|---|---|
| P0 工程骨架 | 已完成 | `src/`、测试、ruff、pre-commit、CI、gitignore、uv 环境 |
| P1 UI 兜底 | 已完成首版 | 人工月范围、展开、清除未结业、查询、导出、文件校验与运行记录入口 |
| P2 API PoC | 已完成只读实测 | 课消汇总查询/导出已确认；排课列表进一步确认 `POST next.xiaogj.com/api/course/Course/QueryNew` 返回分页 JSON 明细，采集器已接入并自动翻页，页面真实鉴权头不落盘 |
| P3 标准数据层 | 首版完成 | `ScheduleRecord` 兼容 API/Excel 别名，日期、小时数和范围校验有测试 |
| P4 指标引擎 | 口径已对齐，业务规则有阻塞项 | 周平均课次、平均课时（总课时/总单科数）、计划/预排课时、生产课时、人工月平均和预排快照已分开实现；取消/作废课已排除 | `HUMAN_REQUIRED / BUSINESS_RULE_BLOCKED`：赠送课、亲属赠送、排课专用课程等缺少可靠排除字段，不能猜测生产口径 |
| P5 PostgreSQL | schema + 幂等 writer 完成 | 迁移和 upsert 已就绪；`--write-metrics` 同时写入指标和带 `snapshot_date` 的预排快照；尚未连接实际服务器 |
| P6 Metabase | 看板规格完成 | 聚合视图和手机首屏定义已就绪；尚未连接实际实例 |
| P7 定时运行 | 采集调度模板完成；Dashboard 自动刷新 BLOCKED | launchd 仅执行 `--collect-schedule`；已有 JSON→UI 失败回退、JSONL 失败记录和可选 macOS 本机通知 | `HUMAN_REQUIRED / BUSINESS_RULE_BLOCKED`：业务排除规则待确认；实际 launchd 安装和 Dashboard 刷新链路待操作员验证 |

## 当前外部依赖

排课明细 API 已通过 Chrome 只读确认并接入；PostgreSQL、Metabase 和定时通知仍需部署环境与操作员配置。Dashboard 自动刷新当前为 `BLOCKED`；生产 KPI 的赠送课、亲属赠送、排课专用课程排除规则为 `HUMAN_REQUIRED / BUSINESS_RULE_BLOCKED`。认证信息不进入仓库。

逐阶段证据与部署前验收步骤见 `docs/验收清单.md`。

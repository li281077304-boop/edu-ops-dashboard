# HUMAN GATE

1. 手机下载 GitHub Release 里的 `app-debug.apk`。
2. 安装或覆盖旧版本。
3. 打开“经营看板”。
4. 导入 Release 里的 `widget-data.json`，或配置并同步真实 API。
5. 返回主屏刷新或重新添加 Widget。
6. 截图确认：没有“测试数据”；数值来自真实数据；半透明视觉已生效。
7. 关闭网络或电脑，确认 Widget 数字仍然显示。
